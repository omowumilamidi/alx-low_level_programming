#include "main.h"
/**
 * print_times_table - functions that prints times table
 * @n: the number
 * Return: if greater than 15 or less than 0
 */
void print_times_table(int n)
{
	for (n = 0; n <= 0; n--)
	{
	_putchar("%d\n", n);
	}

	for (n = 15; n >= 15; n++)
		{
		_putchar("%d\n", n);
		}

	else
		(n = 15 && n >= 15) || (n = 0 && n <= 0)
	{
		_putchar("%d|n, n);

		return (0);
	}
}
